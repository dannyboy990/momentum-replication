# Headline result (python run_imc.py / python imc.py headline) and analysis
# layer (imc.py build/tc/holding) need only these:
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pyarrow>=12.0

# Full panel rebuild from WRDS (pull_data.py -> build_panel.py) additionally needs:
polars>=0.20          # build_panel.py (streaming panel construction)
wrds>=3.1             # pull_data.py (CRSP / TAQ pulls; requires a WRDS account)
requests>=2.28        # pull_data.py (Ken French data library download)
